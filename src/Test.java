import java.util.Scanner;

public class Test {

    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int n = scanner.nextInt(); // размер стопки a
        int m = scanner.nextInt(); // размер стопки b
        int s = scanner.nextInt(); // максимальное число зарплаты
        int resume = 0;

        int max = n + m;

        int result = 0;

        if (n > m) {
            for (int i = 0; i < n; i++) {

            }
        }
    }
}
